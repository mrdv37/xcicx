import { createStyles, rem } from '@mantine/styles';

export const useTotalTableStyles = createStyles( theme => ( {
	colorDotCounter: {
		marginBottom: rem(11)
	},
	smallNumberCounter: {
		marginTop: rem(25)
	}
} ) );
